# 技术实施方案 — 私域枢纽（PrivHub）文件管理系统

> 本文档是动手写代码的施工图，基于已确认的需求、架构、前端原型，以及对本机 DSH 源码与现有插件的实地调研。
> 调研基准：`F:\program3\deepseek harness\`（DSH 主源码 + plugins/）。
> 项目最终形态：**完全独立的程序**（复用一份开源引擎底座，与 DSH 无关，如「红警2/红警3」）。

---

## ✅ 阶段一已完成（里程碑）

**成果**：底座 + 登录系统 + 预置账号 + 局域网访问，全部打通。启动验证输出：

```
dsh web: http://127.0.0.1:3180 (LAN: http://192.168.18.26:3180)
```

### 已打通的能力

| 能力 | 接口/形态 | 状态 |
|------|-----------|------|
| 登录校验 | POST `/privhub/api/login` | ✅ 错误密码正确返回 401 |
| 当前用户 | GET `/privhub/api/me` | ✅ |
| 列目录 | GET `/privhub/api/list` | ✅ 返回 A项目/B项目/公共 |
| Vue 前端 | 登录页 + 四区布局 | ✅ 正常渲染（200） |
| 局域网访问 | bind 0.0.0.0 | ✅ LAN: 192.168.18.26:3180 |

### 预置账号

| 用户名 | 密码 | 角色 |
|--------|------|------|
| admin | admin123 | 管理员 |
| user1 | user123 | 普通用户 |

### 落盘结构（全部在 `F:\program\dsh-SQL\privhub\`）

```
privhub/
├── home/profiles/privhub/     ← 独立 DSH_HOME + privhub profile
│   ├── package.json           ← bundles: dsh-base + dsh-web-app + privhub-server
│   ├── cordis.patch.yml       ← 禁对话UI + webserver 绑 0.0.0.0:3180
│   └── pnpm-workspace.yaml
├── privhub-app/
│   ├── server/                ← 后端接口插件（登录/me/列目录）
│   │   ├── package.json        ← dsh.bundle.patch
│   │   ├── cordis.patch.yml    ← insert privhub-server
│   │   └── src/index.ts        ← webServer.register 三个接口
│   ├── frontend/index.html     ← Vue 前端（登录 + 四区布局）
│   └── data/users.json         ← 预置 admin/user1
├── shared/                    ← 共享目录（A项目/B项目/公共）
└── （底座源码，复制自 DSH）
```

### 关键改造（底座成为我们自己的程序后允许修改）

1. `packages/bundle/web-app/src/startup.ts`：放开 `0.0.0.0` bind 限制（允许局域网访问）。
2. `packages/bundle/web-app/src/index.ts`：`resolveDistIndex()` 支持 `PRIVHUB_FRONTEND_DIR` 环境变量覆盖，指向我们的 Vue 页面。

### 启动命令

```sh
set DSH_HOME=F:\program\dsh-SQL\privhub\home
set PRIVHUB_FRONTEND_DIR=F:\program\dsh-SQL\privhub\privhub-app\frontend
pnpm dsh --profile privhub --port 3180 --host 0.0.0.0
```

> 开发环境用 3180，生产（宿主机）用 3181。

---

## 0. 调研结论（实地核对的底座插件真机制）

依据本机实际代码（非臆想）：

1. **分层配置模型**：Profile / Bundle / Patch 四层合成插件树。插件靠 `package.json` 的 `dsh.bundle.patch` 字段声明挂载，`cordis.patch.yml` 里 `insert` 一行（`id` + `name`）即完成装配。
2. **前端宿主机制**：`web-app` bundle 通过 `frontend-static` 插件 + `webServer.registerFallback` 托管 dist（SPA），dist 位置由 `resolveDistIndex()` 决定（我们已改为可用环境变量覆盖）。
3. **HTTP 接口注册**：`ctx.webServer.register({ kind: 'exact'|'prefix', path, handler })`，handler 为 `(req: IncomingMessage, res: ServerResponse) => void|Promise<void>`，通过 `ctx.effect()` 注册（可逆）。
4. **服务键**：`ctx.webServer`、`ctx.fs`、`ctx.effect`、`ctx.provide` 等。
5. **模块解析**：profile 目录的插件包名通过 `healProfilesModuleFallback` 把安装依赖闭包 symlink 到 `$DSH_HOME/profiles/node_modules`，从而可解析 in-box 插件。

---

## 1. 总体技术路径（已定）

**复用引擎基础设施，产出纯粹的文件盘程序（薄复用）。**

- 复用：底座 `dsh-base`（基础服务）+ `dsh-web-app`（webserver 等）+ `webServer.register` HTTP 能力。
- 替换：禁掉 AI 对话 UI，塞入我们自己的 Vue 文件盘 + 登录/文件接口。
- 前端：Vue（登录页 + 顶栏/左树/中面板/右条 四区布局）。
- 后端：`privhub-server` 插件，webServer 注册 HTTP 接口。

---

## 2. 插件清单

| 插件 | 职责 | 状态 |
|------|------|------|
| `privhub-server` | 登录/当前用户/列目录接口 | ✅ 阶段一完成 |
| `privhub-frontend`（Vue） | 文件盘界面 | ✅ 阶段一完成（登录+四区骨架） |
| `privhub-file-*`（上传/下载/增删改/权限） | 阶段二 | 待做 |
| 细粒度权限/预览/审计 | 阶段三 | 待做 |

---

## 3. 分阶段计划

- **阶段一（已完成）**：底座 + 登录 + 预置账号 + 局域网访问。
- **阶段二（下一步）**：文件核心操作（上传/下载/新建文件夹/复制移动重命名删除）+ 回收站 + 项目级权限隔离。
- **阶段三**：在线预览、细粒度权限、审计日志、版本历史。

---

## 4. 端口与部署

| 场景 | 端口 |
|------|------|
| 本机现有 DSH | 3080（占用） |
| 私域枢纽开发 | 3180 |
| 私域枢纽生产（宿主机） | 3181 |

部署：整个 `privhub/` 目录（含底座 + 我们的插件 + 前端 + 数据）通过向日葵/网盘传到宿主机，改端口为 3181 启动即用。

---

## 5. 待办 / 备忘

- [ ] 阶段二：文件上传/下载/增删改接口 + 前端交互。
- [ ] 密码哈希强化（当前 SHA-256 从简，生产换 bcrypt/argon2）。
- [ ] 会话持久化（当前内存 Map，重启失效，生产需持久）。
- [ ] 处理 web-app 注入的 `window.__DSH_BOOT__` 残留（无害但可清理）。
- [ ] 项目正式名确认为「私域枢纽 PrivHub」后，检查所有命名统一。

---

*生成：会话技术方案确认与阶段一完成*
