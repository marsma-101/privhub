# privhub-core bundle patch：作为 bundle 挂载（insert 即生效）
# usersFile / dataRoot 留空：由核心服务按项目根（PRIVHUB_ROOT）计算默认值。
- insert:
    - id: privhub-core
      name: privhub-core
      config:
        usersFile: ''
        dataRoot: ''
