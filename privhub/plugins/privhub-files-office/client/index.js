// privhub-files-office · client — 纯后端插件（无 UI 组件）
// 提供 /api/office-preview 服务端提取；前端预览由 privhub-files-preview 消费。
// 空 client：满足 shell manifest 挂载约定（slots 为空）。
export default {
  id: 'privhub-files-office',
  slots: {},
}
