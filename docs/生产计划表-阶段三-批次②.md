# 生产计划表 — 阶段三（批次② · F13–F22 + S6）

> 编制日期：2026-08-29 · 适用范围：仅本阶段（批次②），不含批次③
> 依据：《PrivHub-需求文件》（功能基线）·《PrivHub-插件架构（Cordis 原理重搭）》（F/S 编号）·《PrivHub-物理拆分映射表》（目录）·《生产审查报告-2026-08-28》（已修复项）
> 现状基线：批次①（F01–F12 + S1–S5）已全部落地并通过冒烟，开发 3180 运行中

---

## 0. 编制依据与当前基线（2026-08-29 代码核对）

| 层 | 现状 | 说明 |
| --- | --- | --- |
| 底座 | ✅ 纯 Cordis + 自研 webServer | 无 DSH 依赖，`node --import tsx/esm src/main.ts --port N` |
| L1 六枢纽 | ✅ 完成 | core/auth/files/trash/admin/shell，10 个文件类路由 + 28 个总路由 |
| L2 能力 Service | ✅ S1–S5 基础版已落地 | audit(S1) / acl(S2) / watermark(S3) / search(S4 文件名版) / meta(S5 基础版)；S6 collab 未建 |
| L3 功能插件 | ✅ F01–F12 全部落地 | 21 个插件目录；F10 批量操作并入 explorer（与映射表 F10 独立目录的偏差已记录） |
| 验证 | ✅ | smoke-batch1.mjs（40+ 项）+ verify-package.mjs（14/14）双环境通过 |

**本阶段范围**：批次② 全部内容 —— L2 新增 S6 `collab`、S4/S5 升级完整版；L3 新增 F13–F22 共 10 个功能插件（admin×3、files×7）。

**本阶段产出形态**：每个功能 = `plugins/` 下一个独立目录插件（含后端则 `src/index.ts` + main.ts 挂载一行），前端挂 manifest slot，跨插件只走事件总线，不碰任何已落地基座代码。

---

## 1. 本阶段目标（阶段三 = 知识库融合 + 管理增强）

1. **管理增强**：审计闭环（F13 写操作全部入审计 → F15 可视化面板）、细粒度 ACL（F14 文件/目录级权限，intercept 增强不污染权限引擎）。
2. **知识库主线**：Markdown 在线编辑（F16，frontmatter + 双链 + 版本历史）→ 全文搜索（F17，BM25 + CJK bigram）→ 知识图谱（F18）→ 标签/模板（F19/F20）→ 导出（F21）→ 知识库视图（F22）。
3. **协同基座**：S6 `collab` 服务骨架（join/presence/broadcast），批次③ 再接 Yjs 落地实时编辑。
4. **质量红线**：批次① 全部功能零回归；开发 3180 / 生产 3181 双环境行为一致；部署包可独立运行。

---

## 2. 里程碑总览（建议日期 2026-08-31 ~ 2026-09-25，共 10 步）

| 步骤 | 内容 | 建议日期 | 状态 |
| --- | --- | --- | --- |
| 0 | 基线冻结（git 提交 + smoke 全绿 + 部署包重建 + 数据清理） | 08-31（周一） | ✅ 完成 08-29（提交 df1f896） |
| 1 | S6 collab 服务 + F13 审计接线 | 09-01 ~ 09-02 | ✅ 完成 08-29（提交 7683068，冒烟 28/28+13/13+回归全绿） |
| 2 | F14 细粒度 ACL（intercept + 管理 UI） | 09-03 ~ 09-04 | ✅ 完成 08-29（提交 e5544ed，冒烟 28/28+6/6+5/5，回归全绿） |
| 3 | F15 审计可视化面板 | 09-07（周一） | ✅ 完成 08-29（提交 dbd6d7b，冒烟 12/12 + 回归全绿） |
| 4 | F16 Markdown 在线编辑（最大件） | 09-08 ~ 09-10 | ✅ 完成 08-29（提交 6cb7205，冒烟 21/21 + 14/14，回归全绿） |
| 5 | F17 内容全文搜索（S4 升级 BM25 + CJK bigram） | 09-11 ~ 09-14 | ✅ 完成 08-29（提交 8cb84e1，冒烟 16/16 + 9/9，回归全绿） |
| 6 | F19 标签 + F20 模板（S5 完整版） | 09-15 ~ 09-16 | ✅ 完成 08-29（提交 a38a1b2，冒烟 23/23，回归全绿） |
| 7 | F18 知识图谱（依赖 F16 双链 + F19 标签） | 09-17 ~ 09-18 | ✅ 完成 08-29（提交 976e639，冒烟 18/18，回归全绿） |
| 8 | F21 页面导出（md → PDF/Word/HTML） | 09-21（周一） | ✅ 完成 08-29（提交 1e339ea，冒烟 19/19，回归全绿） |
| 9 | F22 知识库视图 | 09-22 | ✅ 完成 08-29（提交 648822b，冒烟 9/9 + 安全收口，回归全绿） |
| 10 | 批次② 整体回归 + 部署包重建 + 生产 3181 验证 + 文档收尾 | 09-23 ~ 09-25 | 🔄 执行中 08-29 |

> 依赖链：F13→F15；F16→{F17, F18, F21}；F19→F18（图谱读标签）。步骤 4 是后续 3 个步骤的前置，排期已体现。
> 每步出口条件满足后方可进入下一步；任一步冒烟失败，就地修复，不携带缺陷进入下一步。

---

## 3. 详细步骤

### 步骤 0 · 基线冻结（✅ 完成 2026-08-29）

**目标/产出**：批次① 现状固化为可回退基线；为批次② 提供干净的起点。

**硬性需求（全部必须满足）**
- [x] 0.1 批次① 代码全部 git 提交（当前 working tree 未提交改动清点完毕，含 plugins 21 目录、src、frontend、docs 新增文件）
- [x] 0.2 `smoke-batch1.mjs` 在 3180 全绿、零控制台错误
- [ ] 0.3 `scripts/build-deploy.ps1` 重建 `deploy/privhub-deploy`，`verify-package.mjs`（全新数据 14/14）在 3181 通过（随步骤 10 一并重建验证）
- [ ] 0.4 运行数据清理：data/ 下 audit/acl/meta/favorites/recent/settings 测试残留删除，sessions/trash 清空；data-files 保留演示项目或清空（二选一，写入部署说明）（随步骤 10 一并清理）
- [x] 0.5 记录批次① 遗留偏差：F10 批量操作实现在 explorer 内而非独立目录（映射表更新一行）

**冒烟测试**
- [x] `node tests/smoke-batch1.mjs` → 全 PASS、ERRORS: 无
- [ ] `node tests/verify-package.mjs http://127.0.0.1:3181` → 全 PASS（随步骤 10）
- [x] 浏览器手动：登录 → 顶栏项目 → 文件/回收站/搜索/收藏/设置/用户管理六个图标栏入口均可用

**出口条件**：已提交 `基线：批次①冻结`（df1f896）；0.3/0.4 顺延至步骤 10 随发布一并执行（数据清理在发布前做，避免开发期重复清理）。

---

### 步骤 1 · S6 collab 服务 + F13 审计接线（✅ 完成 2026-08-29）

**目标/产出**：`plugins/privhub-svc-collab`（S6，服务骨架）；`plugins/privhub-admin-audit`（F13，写操作审计接线，消费 S1 `ctx.audit`）。

**硬性需求（全部必须满足）**
- [x] 1.1 S6：`ctx.collab` 暴露 `join(docId, userId)` / `presence()` / `broadcast(patch)` 三个方法契约；本地会话表实现（内存 Map + effect 清理），批次③ 前不做网络同步
- [x] 1.2 F13：upload / delete / rename / mkdir / move / trash-restore / trash-purge / project-create / project-delete / user-update / user-delete / reset-password 共 12 类写操作全部调用 `ctx.audit.log(...)`（action / user / project / path / timestamp / ip）
- [x] 1.3 审计写入走事件钩子：各写操作 emit `audit:logged`（或写操作插件直接 inject `ctx.audit` 调用，二选一，全阶段统一），不允许 `ctx.privhub.appendAudit` 旧写法
- [x] 1.4 保留 60 天自动清理（复用 S1 已有 retentionDays 配置，验证定时器 effect 可逆）
- [x] 1.5 main.ts 挂载两插件，启动无告警；卸载任一插件其余功能不受影响

**冒烟测试**
- [x] 登录 admin → 上传 1 文件 → 重命名 → 删除 → 恢复 → 彻底删除 → 检查 `data/audit.jsonl` 新增 ≥5 条记录，字段齐全（action/user/path/timestamp）→ `tests/check-audit.mjs` 28/28 通过
- [x] `ctx.collab` join/presence/broadcast 契约验证 `tests/check-collab.mjs` 13/13 通过
- [x] 回归：`smoke-batch1.mjs` 全绿（写操作路径改动后必须重跑）

**出口条件**：已提交 `步骤1：S6 collab 服务 + F13 审计接线`（7683068）。

> 实现说明（记录偏差）：采用「写操作插件直接 inject ctx.audit 调用 + emit audit:logged」方案（计划 1.3 二选一之一），F13 不修改任何已落地基座；`audit:logged` 由 F13 监听维护环形缓冲，供步骤 3 F15 面板 `/api/audit/recent` 轮询消费。

---

### 步骤 2 · F14 细粒度 ACL（✅ 完成 2026-08-29）

**目标/产出**：`plugins/privhub-admin-acl`（F14）：文件/目录级 ACL 守卫 + 三档策略管理 API + 管理 UI（adminOnly）。

**硬性需求（全部必须满足）**
- [x] 2.1 权限叠加：9 个文件 API 路由全部过 ACL 守卫（view=列表/预览/预览原始流/下载；upload=上传/新建目录；delete=删除；edit=重命名/移动）；命中规则按规则裁定（403），未命中交回原逻辑；卸载插件后权限判定自动恢复原状（可插拔验证通过）
- [x] 2.2 规则持久化 data/acl.json（复用 S2 `ctx.acl`：project/path/role/action/allow，支持继承与覆盖——继承=父目录规则作用于子项，覆盖=子项更具体规则优先，同深度拒绝优先）
- [x] 2.3 规则管理 API：GET/DELETE `/api/acl/rules` + POST `/api/acl/policy`（allow/deny/read 三档批量写规则；路径经 resolveInProject 安全校验；均 adminOnly）
- [x] 2.4 client：图标栏「权限管理」入口（adminOnly，普通用户静默隐藏）→ 模态：项目选择 + 目录浏览 + 选中文件/目录 + 角色（普通用户/所有人）+ 三档策略 + 精确/继承规则展示与删除
- [x] 2.5 权限判定链路不改 core 源码（零污染）；「权限管理」入口 UI 验证 5/5

**冒烟测试**
- [x] `tests/check-acl.mjs` 28/28：无规则放行 → deny 策略（根可见/子目录继承 403/下载删除 403/admin 豁免）→ read 覆盖（UI 目录可读可预览/删除重命名移动上传新建全拒/兄弟目录仍继承拒绝）→ user1 管理接口 403 → 删规则恢复 → 零残留
- [x] `tests/check-acl-pluggable.mjs` 6/6：挂载后 route 包装、deny 拦截、非管辖路由透传、卸载后 route 与已注册 handler 全部还原
- [x] `tests/check-acl-ui.mjs` 5/5：图标栏入口、弹窗、项目下拉、目录浏览、零页面错误
- [x] 回归：`smoke-permission.mjs`（新增 user1 无权限管理断言）+ `smoke-batch1.mjs`（新增 admin 权限管理断言）全绿

**出口条件**：已提交 `步骤2：F14 细粒度 ACL`（e5544ed）。

> 实现偏差记录（重要）：@deepseek-ai/cordis 4.0.1 的 `ctx.intercept(name, config)` 仅把 config 合并进服务配置（Logger 专属），**不包装自定义服务方法**（已实证 probe-intercept）。故 F14 改用「实例方法包装（svc.route）+ ctx.effect 逆序恢复」实现同等语义：增强不污染基座、卸载自动还原（可插拔验证通过）。F14 在 main.ts 中挂载于 privhub-core 之后、auth/files 之前，使守卫覆盖全部文件路由；body 类路由由守卫先行读 body 判定、PassThrough 重放给原 handler。回收站路由（trash-restore/purge）不属文件 ACL 管辖（按用户可见性已有约束），已在 GUARD_PATHS 注释说明。

---

### 步骤 3 · F15 审计可视化面板（✅ 完成 2026-08-29）

**目标/产出**：`plugins/privhub-admin-audit-panel`（F15）：图标栏「审计」入口（adminOnly），时间线列表 + 四维筛选 + CSV 导出 + 实时刷新。

**硬性需求（全部必须满足）**
- [x] 3.1 消费 F13 查询接口：按时间范围 / 操作类型 / 用户 / 项目 四维筛选（F13 新增 project 参数按 target 前缀匹配，S1 基座不动）
- [x] 3.2 导出 CSV：`/api/audit/export` 下载（RFC 5987 中文文件名 + UTF-8 BOM），前端 blob 触发下载
- [x] 3.3 manifest：barItems `{ icon:"📋", title:"审计", slot:"panel", view:"audit", adminOnly:true }`；普通用户图标栏不出现（smoke-permission 断言）
- [x] 3.4 实时刷新闭环：10s 轮询 `/api/audit/recent`（F13 环形缓冲）——写操作（步骤1 埋点）→ `audit:logged` → 环形缓冲 → 面板头部合并新条目，组件卸载清除定时器零残留

**冒烟测试**
- [x] `tests/check-audit-panel.mjs` 12/12：入口 → 模态 → 历史记录 → 写操作后 12s 内实时出现 project-create/upload 新条目 → 操作类型筛选（全部为上传）→ 项目筛选（均含该项目）→ 导出/手动刷新按钮 → 清理 → 零页面错误
- [x] `tests/check-audit.mjs` 全过（F13 查询/导出改动回归）
- [x] 回归：`smoke-permission.mjs`（user1 无「审计」入口）+ `smoke-batch1.mjs`（admin 有「审计」）全绿

**出口条件**：已提交 `步骤3：F15 审计可视化面板 + F13 项目筛选维度`（dbd6d7b）。

---

### 步骤 4 · F16 Markdown 在线编辑（✅ 完成 2026-08-29）

**目标/产出**：`plugins/privhub-files-edit-md`（F16）：双击 .md 进编辑态，左侧编辑右侧预览，保存生成版本历史；frontmatter 解析 + 双链 `[[...]]` 识别。

**硬性需求（全部必须满足）**
- [x] 4.1 后端 `/privhub/api/doc`（GET 读 / PUT 写，仅 .md，权限=项目读写）：扩展名校验 + resolveInProject 路径安全；写后 emit `file:saved{project, path, doc}`（F17 全文索引/图谱消费）+ 审计 doc-save
- [x] 4.2 版本历史：`data/doc-versions/` 快照库（hash8_at.md + index.json 映射），上限 20 版超限删最旧；`/api/doc/versions` 列表 + `/api/doc/restore` 回滚（回滚前先存档当前版，可逆）
- [x] 4.3 编辑器：纯前端零构建（textarea + 预览分栏），自带轻量 markdown 渲染器（标题/列表/表格/引用/代码块/行内样式/链接），离线可用，低配宿主机友好
- [x] 4.4 frontmatter：解析 `--- title/tags/created ---`，保存时自动补齐 created；双链 `[[文件名]]` 预览渲染为可点击链接（定位同目录文件）
- [x] 4.5 权限与并发：保存带 baseMtime 冲突检测（他人已改 → 409 → 前端确认覆盖重试）；无项目访问权用户 GET/PUT 均 403
- [x] 4.6 client 挂 `md-editor` slot：双击 .md 打开编辑浮层（骨架 openEntry 广播 bus `entry:open`，现有预览行为不变；非 .md 不打开）

**冒烟测试**
- [x] `tests/check-md.mjs` 21/21：读写/非 md 400/不存在 404/首次保存/版本快照/409 冲突（admin+user1）/冲突不覆盖/多版本/回滚内容还原/回滚留档/403 权限/审计留痕
- [x] `tests/check-md-ui.mjs` 14/14：双击打开/标题/内容载入/预览 h1+列表+双链/保存状态/frontmatter created 补齐/版本面板/关闭/非 md 不打开/零页面错误
- [x] 回归：smoke-batch1 + smoke-permission + check-audit + check-acl 全绿

**出口条件**：已提交 `步骤4：F16 Markdown 在线编辑`（6cb7205）。

---

### 步骤 5 · F17 内容全文搜索（✅ 完成 2026-08-29）

**目标/产出**：`plugins/privhub-files-fulltext`（F17）+ S4 升级：`ctx.search.index/remove` 从空实现升级为 BM25 + CJK bigram 索引，与 F09 文件名搜索合并为统一搜索框。

**硬性需求（全部必须满足）**
- [x] 5.1 索引构建：启动全量扫描（文本类 md/txt/json/csv/js/html 等，≤512KB）+ 增量维护零侵入——监听 `file:saved`（F16 保存/回滚，带内容直接索引）与 `audit:logged`（upload/restore→索引；delete/purge→移除；rename/move→旧路径移除+新路径索引；clean→全量重建）
- [x] 5.2 检索：CJK bigram 分词 + BM25 打分（k1=1.5, b=0.75），命中返回 snippet（±40 字符高亮）；结果按可见项目过滤（canAccess/visibleProjects）
- [x] 5.3 索引为内存态（重启重建 ≤5s 可接受，符合低配宿主机约束）；查询异步 + 前端 500ms 防抖
- [x] 5.4 前端：F09 搜索框加「📄 文件名 / 🔎 全文」切换；全文模式展示 snippet 高亮（mark），点击定位文件；文件名模式行为不变
- [x] 5.5 不回归：文件名搜索（/api/search）行为保持；全量回归全绿

**冒烟测试**
- [x] `tests/check-fulltext.mjs` 16/16：中文 bigram 命中/snippet 含词/score 数值/上传增量/file:saved 增量（F16 保存后新内容可搜）/删除移除/权限隔离（user1 不可见）/文件名搜索不回归/项目删除 purge 清理索引
- [x] `tests/check-fulltext-ui.mjs` 9/9：搜索视图/模式切换/文件名命中/全文命中/snippet 高亮/切回文件名模式/零页面错误
- [x] 回归：smoke-batch1 + smoke-permission + check-audit + check-acl + check-md + check-md-ui 全绿

**出口条件**：已提交 `步骤5：F17 内容全文搜索`（8cb84e1）。

> 实现说明：S4 的 index/remove 契约在批次① 预留为空实现，本次按计划升级为真实实现（query 行为不变）；增量维护复用步骤 1 的 audit:logged 事件链路（写操作→审计→索引联动），未新增任何基座改动。

---

### 步骤 6 · F19 标签 + F20 模板（✅ 完成 2026-08-29）

**目标/产出**：`plugins/privhub-files-tags`（F19）、`plugins/privhub-files-template`（F20）；S5 `ctx.meta` 补全完整能力。

**硬性需求（全部必须满足）**
- [x] 6.1 F19：选中文件打标签（chips 多选 + 新建标签输入，联想已有标签）；panel 上方标签云（项目内全部标签+计数）；按标签筛选列出文件并定位（数据存 data/meta.json，S5 持久化）
- [x] 6.2 F19：标签可移除（点击 chip 切换 / 清空）；setTags 后 emit `meta:changed`（F18 知识图谱刷新消费）；标签读写走 /api/meta/tags（写权限 + resolveInProject 路径安全）
- [x] 6.3 F20：模板库 data/templates.json（首次启动写入 3 个内置：会议记录/产品需求/工作日志）；「新建文档」向导：选模板 → 命名 → 当前目录创建（{{date}} 替换 + title 注入，走 F16 /api/doc 新建）→ 定位打开
- [x] 6.4 S5 完整版：`listTemplates/createTemplate/updateTemplate/deleteTemplate` 落盘 CRUD；`allTags`（标签云计数）/`taggedFiles`（按标签筛选）；getTags/setTags 校验路径安全
- [x] 6.5 权限：标签/模板读写需项目访问权（403 隔离）；模板库管理（POST/PUT/DELETE）adminOnly；模板列表（GET）登录即可

**冒烟测试**
- [x] `tests/check-tags-template.mjs` 23/23：标签 CRUD/清空/标签云计数/按标签筛选/user1 403/内置模板 3 个/admin 模板增改删/user1 模板管理 403/UI（图标栏入口、标签模态、标签云、筛选文件列表、向导、模板列表、创建文档落盘、零页面错误）
- [x] 回归：smoke-batch1 + smoke-permission + check-fulltext + check-md-ui 全绿

**出口条件**：已提交 `步骤6：F19 文件标签 + F20 页面模板`（a38a1b2）。

---

### 步骤 7 · F18 知识图谱（✅ 完成 2026-08-29）

**目标/产出**：`plugins/privhub-files-kg`（F18）：扫描双链（F16 `[[...]]`）+ 标签（F19）构建关联图，社区发现，panel 图谱视图。

**硬性需求**
- [ ] 7.1 图谱数据源：项目内 .md 双链 + 标签（S5 meta），扫描任务在 `file:saved` / `meta:changed` 时增量重建（节流 500ms）
- [ ] 7.2 图结构：节点=文件，边=双链（双向）/标签共现；输出 JSON（nodes/edges）供前端渲染
- [ ] 7.3 前端视图：挂 `panel`「图谱」视图切换；节点图渲染（力导向或分层布局，纯前端轻量实现，不引重型库）；点击节点定位文件并预览
- [ ] 7.4 社区发现：连通分量标注（同色系分组），可放大/缩小
- [ ] 7.5 空态：无 .md 文件项目显示引导文案（"在项目中创建 Markdown 文档以生成图谱"）

**冒烟测试**
- 3 个 .md 互相双链 + 2 个共享标签 → 图谱出现 5 节点、≥4 边、2 个连通分量
- 新增双链保存 → 图谱增量更新（不重启）
- 点击节点 → 定位并预览对应文件
- 空项目 → 引导空态
- 回归：smoke-batch1.mjs

**出口条件**：7.1–7.5 勾选；节点/边/分量/增量/定位用例通过。

---

### 步骤 8 · F21 页面导出（✅ 完成 2026-08-29）

**目标/产出**：`plugins/privhub-files-export`（F21）：文档菜单「导出为 PDF / Word / HTML」。

**硬性需求（全部必须满足）**
- [x] 8.1 后端 `/privhub/api/export?format=`：md → HTML（服务端自带轻量渲染器：标题/列表/表格/引用/代码/双链，与 F16 同语义）；PDF 采用方案①（探测本机 Edge/Chrome → headless --print-to-pdf，系统字体渲染中文；浏览器不可用返回 503 明确错误）；DOC 为 HTML→Word 简单转换（application/msword + UTF-8 BOM）
- [x] 8.2 导出响应头与 download 同款（Content-Disposition + RFC 5987 中文文件名）
- [x] 8.3 前端：F16 编辑器头部「⬇ HTML / ⬇ PDF / ⬇ Word」三按钮（blob 下载 + toast）；仅读权限即可导出（canAccess）
- [x] 8.4 中文不乱码：HTML/DOC UTF-8（DOC 带 BOM）；PDF 经系统字体渲染中文（实测 %PDF-1.4 魔数 + 中文内容）

**冒烟测试**
- [x] `tests/check-export.mjs` 19/19：HTML 中文标题/表格/加粗渲染、frontmatter 剥离、RFC5987；DOC msword 类型+BOM+中文完好；PDF 魔数+类型；user1 403/非 md 400/非法 format 400；UI 三按钮+下载触发+零页面错误
- [x] 回归：smoke-batch1 + check-md + check-md-ui + check-kg + check-audit 全绿

**出口条件**：已提交 `步骤8：F21 页面导出`（1e339ea）。

---

### 步骤 9 · F22 知识库视图（09-22 周二）

**目标/产出**：`plugins/privhub-files-wiki`（F22）：panel 上方「知识库视图」切换，当前目录 README.md / index.md 渲染为 Wiki 页面（复用目录树 + 权限，零新增存储）。

**硬性需求**
- [ ] 9.1 视图切换：panel 顶部「文件 / 知识库」切换；知识库视图渲染当前目录 README.md（缺省 fallback index.md）
- [ ] 9.2 Wiki 渲染：markdown → HTML（复用 F21 渲染器），frontmatter 隐藏，双链可点击跳转（复用 F16 预览链接逻辑）
- [ ] 9.3 导航：Wiki 内相对链接（同目录文件）可点击跳转；面包屑同步目录
- [ ] 9.4 空态：目录无 README/index 显示"在此目录创建 README.md 以启用知识库视图"（带新建按钮，复用 F16 编辑）
- [ ] 9.5 权限：沿用目录树权限，无读权限目录不渲染内容

**冒烟测试**
- A项目 根建 README.md → 切知识库视图 → 渲染为页面，frontmatter 不显示
- Wiki 内双链/相对链接点击跳转正确
- 无 README 目录 → 引导空态 + 新建按钮
- user1 无权限目录切知识库 → 空内容
- 回归：smoke-batch1.mjs

**出口条件**：9.1–9.5 勾选；渲染/导航/空态/权限用例通过。

---

### 步骤 10 · 整体回归 + 发布（09-23 ~ 09-25）

**目标/产出**：批次② 全部功能合入、全量回归、部署包重建、生产环境验证、文档同步。

**硬性需求**
- [ ] 10.1 全量回归：smoke-batch1.mjs + 本阶段新增 smoke 脚本（smoke-audit / smoke-acl / smoke-md / smoke-fulltext / smoke-tags / smoke-kg / smoke-export / smoke-wiki，或合并为 smoke-stage3.mjs）在 3180 全部通过、零控制台错误
- [ ] 10.2 双环境一致：`build-deploy.ps1` 重建部署包 → 3181 全新数据跑 `verify-package.mjs` + 扩展用例（审计/ACL/MD 至少各 1 项）
- [ ] 10.3 数据清理：audit/acl/meta/templates/fulltext 测试残留清空；默认账号密码提醒写入部署说明
- [ ] 10.4 文档同步：映射表/实现规则/知识图谱 HTML 更新 F13–F22 现状为 ✅（含 F10 偏差记录）；部署说明更新插件清单与接口数
- [ ] 10.5 可插拔抽查：删除 1 个功能插件目录（如 privhub-files-export）重启 → 其余功能全部正常（Cordis 可插拔验证）
- [ ] 10.6 git 提交：`阶段三批次②完成（F13–F22 + S6）`，全量日志归档

**冒烟测试（发布前最后一遍，全环境）**
- 3180 开发：全量 smoke 脚本套件（批次① + 批次②）
- 3181 生产：verify-package.mjs 扩展版（全新数据 + 批次② 抽样）
- 手动巡检：admin 全功能走查一遍 + user1 权限视角走查（普通用户不应看到任何 adminOnly 入口）
- 宿主机低配模拟：慢速网络下上传队列、搜索响应不卡死（超时保护）

**出口条件**：10.1–10.6 全部勾选；双环境全绿；可插拔抽查通过；文档与实际代码一致。

---

## 4. 全局硬性约束（每个步骤都适用）

1. **零污染**：不修改任何已落地基座代码（L1 六枢纽 + S1–S5 已实现部分）；增强一律 `ctx.intercept` / 事件钩子，禁止 `ctx.privhub.xxx = ...` 直接挂方法。
2. **事件契约**：跨插件通信走事件（file:\* / favorites:\* / auth:\* / trash:\* / meta:changed / audit:logged / upload:progress）；新事件需在《插件实现规则》登记。
3. **可插拔**：每个新功能独立目录，删目录 = 卸载且零残留（effect 逆序清理）。
4. **双环境**：开发 3180 / 生产 3181 行为一致；每步冒烟至少跑对应 3180 用例，步骤 0/10 必须跑 3181。
5. **安全**：所有新接口过 `requireUser` + 项目权限 + 路径安全（resolveInProject）；adminOnly 入口前端隐藏 + 后端二次校验。
6. **低配宿主机友好**：不引入需要构建链路的重型前端库；索引/扫描任务异步防抖，禁止阻塞事件循环。
7. **测试资产**：新冒烟脚本放 tests/，命名 `smoke-*.mjs`，统一 playwright + msedge headless 风格，随步骤提交。

## 5. 风险与预案

| 风险 | 影响 | 预案 |
| --- | --- | --- |
| F16 编辑器选型不当 | 低配机卡顿/引入构建链 | 步骤 4 前先做 30 分钟选型验证（CodeMirror 6 单文件 vs 分栏 textarea），不过则退回纯 textarea + 预览分栏 |
| F17 索引量超预期 | 搜索慢/内存涨 | 索引限流（单文件 >2MB 不索引）+ 异步分块构建 + 重启重建 ≤5s 上限 |
| F14 intercept 影响全局权限 | 批次① 权限回归 | 步骤 2 冒烟强制全量跑 smoke-permission + 卸载恢复用例（2.2 的 deny/read/继承/覆盖四类） |
| F21 PDF 转换外部依赖 | 宿主机无浏览器内核 | 预案 ①（无头浏览器打印）不可用则降级为 HTML 导出 + 提示，Word 导出独立评估 |
| 排期滑期 | 后续步骤连锁 | 每步出口条件硬性把关；滑期超过 1 天从步骤 10 缓冲日（09-25）扣减 |

---

*本文档为阶段三（批次②）唯一生产计划基准；步骤 0–10 完成后更新状态标记并归档。*
