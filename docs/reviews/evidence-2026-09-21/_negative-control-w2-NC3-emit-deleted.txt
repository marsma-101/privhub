
── 渲染环境 ──
  ✅ Vue 编译器可用（3.5.41）

── A 侧栏：个人空间按普通文件夹显示（用真实姓名） ──
  ✅ 个人空间以「📂 真实姓名」形式出现在项目列表里
  ✅ 不出现「个人空间」字样（用户已明确不要这种形式）
  ✅ 普通项目照常列出
  ✅ 显示的是真实姓名「马泉斌」

── B 顶栏下拉：真实姓名，value 也是真实文件夹名 ──
  ✅ 下拉项显示真实姓名
  ✅ 下拉项不出现「个人空间」字样
  ✅ 下拉项 value 是真实文件夹名「马泉斌」（否则打不开目录）
  ✅ 普通项目 value 正常

── C 目录树根节点：真实姓名 + 🏠 ──
  ✅ 树根显示「🏠 真实姓名」
  ✅ 树根不出现「个人空间」字样

── D 防回退：界面不得引入个人空间专属标签 ──
  ✅ shell 无个人空间专属界面标签残留
  ✅ explorer-v3 无个人空间专属界面标签残留
  ✅ 骨架 index.html 无个人空间专属界面标签残留

── D2 防回退：用户可见的界面文案不得出现「个人空间」 ──
  ✅ 界面文案无「个人空间」字样

── E 通行契约：项目下拉的 value 必须是真实文件夹名 ──
  ✅ 下拉项 value 绑定真实名 p（不得改成展示用标签）
  ✅ 下拉项文本直接渲染 p（真实名）

── F 顶栏层数与重叠（用户反馈：三层顶栏、文件名重复、按钮压到 office 工具条）──
  ✅ 内容区模板中已无任何工具条元素（不占位即不可能与 office 工具条重叠）
  ✅ 样式里也无 .v3-content-head 残留
  ✅ office2 不再以 .v3-content-head 为锚点或样式目标（旧锚点，已改 prepend）
  ✅ 存在「标签行」容器（标签栏 + 操作区同一行）
  ✅ 标签栏存在（文件名以此处为唯一出处）
  ✅ 操作区在标签行内（与标签同行，不叠层）
  ✅ 标签行切片边界有效（切不出内容区）
  ✅ 标签行内不重复渲染文件名（标签本身已显示）
  ✅ 提供单个 ⋯ 菜单
  ✅ ⋯ 菜单承接了 previewFontDec
  ✅ ⋯ 菜单承接了 previewFontReset
  ✅ ⋯ 菜单承接了 previewFontInc
  ✅ ⋯ 菜单承接了 menuCloseCurrent
  ✅ ⋯ 菜单承接了 menuCloseOthers
  ✅ ⋯ 菜单承接了 menuCloseAll
  ✅ ⋯ 菜单承接了 menuCopyPath
  ✅ 右侧详情面板仍有一键开关（保留两侧）
  ✅ 原「✕ 其他」文字按钮已并入 ⋯ 菜单
  ✅ 操作区为定位上下文（菜单锚点）
  ✅ 菜单不挂在 .v3-tabs 上（它 overflow-x:auto，会裁掉下拉）
  ✅ office2 向宿主契约登记自己（window.PrivHub.viewers.register，id = privhub-files-office2）
  ✅ 声明里只报自己的能力（exts = .docx/.xlsx、priority 高于契约默认值），不报「我知道内容区长什么样」
  ✅ 【要害】office2 源码里不再出现 .v3-content：既不查它、也不认领它（迁前那次 document.querySelector 已删除）
  ✅ 【要害·加严】office2 整个文件（含注释）grep 不到 querySelector(".v3-content") —— 0 命中
  ✅ office2 声明里不写 update：同类 docx→docx 换文件时由宿主「先收后建」，绝不复用旧 frame 只改 src（旧画面就是这么露出来的）
  ✅ 组件 beforeUnmount 里 stop()：插件被卸载 → 声明消失 → 零残留
  ✅ office 预览页默认隐藏文件名（避免重复）
  ✅ 单独打开 office 预览页时仍显示文件名（standalone 兜底）

── G 内容区所有权契约（docx 残留防治，方案 §9.2 A1~A4）──
  ✅ 没有冒出来的新注入者（白名单已收敛为空数组）
  ✅ 【第二步 b】office2 已不在「往别人容器里注入」的名单里（它只往宿主给的挂载点里放节点）
  ✅ 【第二步 c】edit-md 已不在「往别人容器里注入」的名单里（它只往宿主给的编辑舱位里放节点）
  ✅ 【第二步 d】comments 已不在「往别人容器里注入」的名单里（它只往宿主交来的渲染根里放锚点）
  ✅ 【验收口径·归零】注入者集合与白名单同时为空（实测 injectors=0 家、白名单 0 条）
  ✅ 除已知注入者（白名单已收敛为空数组）外，没有别家往内容区插节点
  ✅ 宿主存在交接清理方法 clearInjected()
  ✅ 切标签（store.activeKey 变化）时执行交接清理
  ✅ 换文件（store.content.key 变化）时执行交接清理
  ✅ 清理按归属标记认人（data-v3-injected），不按类名认人
  ✅ edit-md 的节点（.md-inline-root）不带归属标记 → 宿主的清理不会碰它
  ✅ comments 的节点（mark 锚点）不带归属标记 → 宿主的清理不会碰它
  ✅ 没有任何插件用“反查他人后代”的写法（.v3-content :has）绑架布局
  ✅ 【第二步 c】宿主样式里也不再按别人的后代反查布局（:has(.md-inline-root) 归零）
  ✅ 【第二步 c】编辑态布局改成宿主自己的状态类 .v3-content--editor（宿主的地宿主管）
  ✅ 宿主提供 viewer 让位布局状态类 .v3-content--viewer
  ✅ 内容区布局类由宿主状态驱动（:class="contentClass"）
  ✅ 让位布局规则写在宿主自己的样式里（宿主的地宿主管）
  ✅ office 专属的硬编码布局分支已彻底消失（宿主不再知道 .docx/.xlsx 是谁的能力）
  ✅ 宿主源码里不再硬编码扩展名能力清单（.docx/.xlsx 只出现在插件自己的声明里）
  ✅ 布局状态由 viewer 会话的返回值派生（谁上场谁给：mount/update/reuse 才让位）
  ✅ contentClass 直接读 viewerLayout（布局状态与「谁上场」是同一条链，不是两套并存的判断）

── H 契约接入：office2 只声明、只往挂载点里放、卸载自己收（跑真身，最小 DOM 桩）──
  ✅ office2 mounted() 里没有任何报错（契约在，插件不喊冤）
  ✅ office2 真身在沙箱里跑了起来，并向契约递交了声明（不是正则猜想，是真跑）
  ✅ 声明 id = privhub-files-office2（全局唯一，惯例用插件 id）
  ✅ 声明 exts = [.docx, .xlsx]（只报自己认领的扩展名）
  ✅ 声明 priority = 200（高于契约默认 100）
  ✅ 声明里没有 update：同类 docx→docx 换文件交给宿主「先收后建」，绝不复用旧 frame 只改 src
  ✅ mount 只往宿主给的挂载点里放了 1 个 iframe（实测挂载点子节点 1 个）
  ✅ 这个 iframe 的父节点就是挂载点本身（不是 .v3-content，也不是别人的节点）
  ✅ iframe 指向本插件的预览页，且 project / path / name 都正确编码（实测：/privhub-plugins/privhub-files-office2/view.html?project=P&path=a%20b.docx&name=a%20b.docx）
  ✅ mount 返回了清理函数（卸载时由宿主调它，插件自己收）
  ✅ 【要害】mount 全程没有按 .v3-content 查过任何东西（本插件不再认领别人的容器）｜实测查询次数 0
  ✅ 那个「别人的容器」（.v3-content）一个节点都没多（本插件没有往里面塞任何东西）
  ✅ 卸载后挂载点里一个节点都不剩（实测 0）
  ✅ 被摘掉的 iframe 已经和 DOM 断开（不是「隐藏起来假装卸载」）
  ✅ 同类换文件时换的是**新节点**（旧节点已脱离 DOM：只改 src 才会在导航期继续显示上一个文件的画面）
  ✅ 组件 beforeUnmount 里 stop()：声明从注册表消失（卸载 = 删目录 → 不再注册 → 零残留）

── I DOM 级行为：宿主交接清场只清带归属标记的节点（跑真身 clearInjected）──
  ✅ 在宿主源码里定位到 clearInjected() 真身
  ✅ 宿主 clearInjected() 真身已取出（真跑，不是正则猜想）
  ✅ 清场后 .v3-content > iframe.office2-frame 数量为 0（实际 0，判据①要求 0）
  ✅ 清场后内容区再无任何带归属标记的残留节点
  ✅ edit-md 的 .md-inline-root 被保留（清理没误伤别家节点）
  ✅ comments 的 mark 锚点被保留（清理没误伤别家节点）
  ✅ 宿主自己的 .v3-pdf iframe 被保留（不带标记就不会被清）
  ✅ 上一次 office 预览留下的内联隐藏被还原（否则 md 预览再也显示不出来）
  ✅ 锚点内部带归属标记的 viewer 被保留（宿主清场跳过挂载点内部，不误杀在岗的 viewer）
  ✅ 没有内容区时 clearInjected() 安全退出（不抛错）

── J 内容区 viewer 契约（注册表 / 锚点 / 挂卸生命周期，跑真身）──
  ✅ viewers.js 真身已在沙箱中取出（真跑，不是正则猜想）
  ✅ 模块加载即建立注册表，且只此一份（window.PrivHub.viewers，重复安装返回同一实例）
  ✅ register 返回注销函数（插件在 beforeUnmount 里调它）
  ✅ resolve 按扩展名解析且大小写/前缀都归一（resolve("docx") 命中；resolve("a.docx") 不命中——它不是文件名解析器）
  ✅ 声明里的扩展名同样归一（" xlsx " → .xlsx）
  ✅ list() 给出只读快照（含 id/exts/priority，不含 mount）
  ✅ 缺 id 当场抛错（不静默降级）
  ✅ 缺 exts 当场抛错
  ✅ 缺 mount 当场抛错
  ✅ priority 不是数字当场抛错
  ✅ 同一个 id 重复注册当场抛错（多半意味着上一次 register 的返回值没在 beforeUnmount 里调用）
  ✅ unregister 之后解析不到（注销契约：声明消失 = 能力消失）
  ✅ 重复 unregister 幂等返回 false（不抛错，便于清理代码无脑调用）
  ✅ 同一扩展名多家声明时 priority 大的先赢
  ✅ 同优先级时【先注册的先赢】（显式定序，不给文件系统顺序留后门）
  ✅ 没有任何声明时 viewer 不上场（回退老路）
  ✅ 无声明时挂载点保持空、标签为 none（宿主自己的 md/text/image/pdf 分支照跑，锚点等于不存在）
  ✅ 有声明时该 viewer 上场（挂进宿主给的锚点，锚点打上 viewer id）
  ✅ 【回退老路】注册后再注销 → viewer 被卸掉、锚点清空、标签归位（假 viewer 注销后老路必须恢复）
  ✅ 收回老路时走的是 viewer 自己的 cleanup（不是宿主硬删节点）｜实测：mount,cleanup
  ✅ 异类从零上场：mount 一次
  ✅ 【同类切换 = 复用】docx→docx 走 update()：不卸载、不重建、没有第二次 mount｜实测：A.mount:P|a.docx , A.update:P|b.docx
  ✅ 【同文件重复同步 = 完全不动】reactivity 多触发几次也不会白闪（没有任何 DOM 动作）
  ✅ 【异类切换 = 换 viewer】锚点里只剩新 viewer 一个（同一时刻只有一个 viewer 活着）
  ✅ 【先卸旧的、再挂新的】顺序可复核｜实测：A.cleanup , B.mount:P|c.md
  ✅ 挂载点元素本身从头到尾是同一个（宿主不重建锚点 = 中间没有空窗）
  ✅ 再切回去同样先卸后挂｜实测：B.cleanup , A.mount:P|a.docx
  ✅ dispose（面板卸载/内容区销毁）时存量 viewer 被收干净 —— 第一步那个残留 bug 的另一个入口也堵上
  ❌ tabs.js：openTab / activateTab / closeTab 都在改 store.activeKey【之前】发保存意图（emitSaveIntent → md:interrupt；保存意图先于挂载点变更）
  ✅ tabs.js 真身已在沙箱中取出（真跑，不是正则猜想）
  ✅ 【顺序准绳·行为】真跑 activateTab：md:interrupt（静默保存）排在 activeKey 变更【之前】｜实测顺序：md:interrupt → activeKey变更
  ✅ 【顺序准绳·行为】真跑 openTab：同样是保存意图先、挂载点变更后｜实测顺序：md:interrupt → activeKey变更 → v3:tab-opened
  ❌ 【顺序准绳·行为】真跑 closeTab：关闭激活标签前同样先发保存意图｜实测顺序：activeKey变更
  ✅ 宿主的内容切换 watcher 不得用 flush:'sync'（panel.js:nav.path 里 activeKey 置空写在 emit 之前，sync 会当场倒挂）
  ✅ activeKey 变化 → 触发 viewer 生命周期（钩子点 H2 就挂在 watcher 上）
  ✅ content 变化 → 触发 viewer 生命周期
  ✅ 面板 beforeUnmount 时把存量 viewer 收干净（内容区整体消失也是卸载入口）
  ✅ 顺序正确的场景真的跑起来了（有 viewer 上场、也被卸掉 —— 断言不是空转）
  ✅ 顺序正确时 checkOrder() 零违规、控制台零报错
  ✅ 【阴性对照·内建】把顺序倒过来（先卸后存）→ checkOrder() 必须报出 1 条违规｜实测：[{"key":"P|a.docx","reason":"no-viewer","unmountSeq":2,"saveSeq":3}]
  ✅ 倒挂会当场 console.error 大喊（本项目的老病正是「连错了不吭声」）
  ✅ 没有任何保存意图的收起路径（menuCloseAll / 切视图）不报违规（不变式只查「倒挂」，不查「有没有发过保存」）
  ✅ 内容区里有宿主创建的挂载锚点 .v3-viewer-host（插件只拿这个 div，不再认 .v3-content）
  ✅ 宿主只【查】锚点、不动态造锚点（锚点由模板创建与销毁）
  ✅ clearInjected 跳过挂载点内部（那是 viewer 的私有财产，由它自己的 cleanup 收；宿主不能顺手删掉在岗的 viewer）
  ✅ 宿主自己的 md / text / image / pdf 渲染分支照旧（回退老路 = 这些分支一条都没动）
  ✅ 契约不在骨架里：frontend/index.html 一字未动（不新增插槽、不改布局、不改图标栏）
  ✅ 契约模块登记在插件装配入口的模块清单里（改哪块只碰哪个文件）
  ✅ 拿到 office2 的真声明（H 段跑出来的同一份，不是测试自己捏的）
  ✅ 真注册表按扩展名解析到 office2 的声明（大小写与前缀都归一：.docx / .XLSX 都命中）
  ✅ 【判据】打开 docx → viewer 上场：宿主给的锚点里出现 1 个 iframe（实测 1 个）
  ✅ 锚点打上了 viewer 身份（data-viewer）且这个 iframe 自报归属（插件不再需要宿主按类名认人）
  ✅ iframe 的父节点是宿主的挂载点（不是内容区本身）
  ✅ 【同类切换不露旧画面】docx→docx：不声明 update ⇒ 宿主在同一锚点里「先收后建」，换的是**新节点**（实测 action=mount，锚点内 1 个 iframe，新旧节点 不同）
  ✅ 这一段里 office2 的 stop() 只在 H 段被调过一次（J6 用的是同一份真声明，没有再注册一遍）
  ✅ 【判据】切到 md → viewer 被卸掉、锚点为净（实测 iframe 0 个，data-viewer=none）
  ✅ 切走后锚点里一个节点都不剩（残留无从谈起）
  ✅ 切到 png（无任何声明的类型）→ 回退老路，锚点仍为空（图片走宿主自己的 img 分支）
  ✅ office2 注销（卸载插件）→ .docx 回退老路（内容区显示宿主的 Markdown 提取预览），零残留
  ✅ 在宿主源码里定位到布局派生的真身（VIEWER_ON_STAGE_ACTIONS + layoutForAction）
  ✅ layoutForAction 真身已取出（真跑，不是正则猜想）
  ✅ viewer 在场上（mount / update / reuse）→ 内容区整块让给它｜实测：mount=viewer update=viewer reuse=viewer
  ✅ 没有 viewer 在场（fallback / unmount / no-host / mount-error）→ 普通布局，宿主自己的分支照跑｜实测：fallback=(空) unmount=(空) no-host=(空) mount-error=(空)
  ✅ J6 实测到的两种 action（打开 docx = mount、切到 png = fallback）分别落在「让位 / 不让位」两侧

── K 编辑舱位：插件不再伸手进宿主的内容区（第二步 c）──
  ✅ 宿主声明编辑舱位选择器 EDITOR_SELECTOR = .v3-editor-host（实测：.v3-editor-host）
  ✅ 舱位由宿主模板创建/销毁（内容区里那个 .v3-editor-host div），插件只往它里面放节点
  ✅ 【要害】edit-md 的 teleport 目标是宿主给的编辑舱位 .v3-editor-host（不再挂到别人的容器上）
  ✅ 【要害】edit-md 的代码里不再认领宿主的内容区容器（剥注释后 0 命中）
  ✅ 【要害·加严】edit-md 整个文件（含注释）grep 不到 querySelector(".v3-content") —— 0 命中
  ✅ 【要害·加严】edit-md 整个文件（含注释）grep 不到 teleport to=".v3-content" —— 0 命中
  ✅ edit-md 不再从整个文档里查任何东西（自己的节点一律 this.$el，别家的一律不问）
  ✅ edit-md 不再按类名去隐藏/还原宿主的只读预览节点（.v3-md / .v3-text）——改由宿主的编辑态布局负责
  ✅ 「现在能不能内嵌」由插件经 bus **问宿主**（不再用「别人的 DOM 非空」当判据）
  ✅ 宿主同步作答：收到 v3:editor-host-ask 就回 v3:editor-host { available, key, selector }
  ✅ 宿主的可用性判据取自**自己的状态**（内容区承载的就是这个文件），不查别人的 DOM
  ✅ 宿主自己查自己的舱位（this.$el + EDITOR_SELECTOR），不是插件去查
  ✅ 插件报「编辑态开/关」→ 宿主写布局状态
  ✅ 编辑态布局状态写进**响应式 data**（editorLayout），不在 computed 里现问
  ✅ contentClass 由 editorLayout 派生（宿主给自己的容器下规矩）
  ✅ 宿主样式里编辑态挂在**自己的状态类** .v3-content--editor 上，`:has(别人的后代)` 已归零
  ✅ 宿主把插件报来的「数据捕获 / 编辑态退场」记进顺序流水（noteEditorLifecycle）
  ✅ 顺序流水用的是 viewers.lifecycle 那一份（与 viewer 的挂载/卸载同一条链）
  ✅ 编辑舱位不进 viewers.js 契约（它是宿主的第二个锚点，不是 viewer 契约的扩展）
  ✅ key 口径取宿主真身（utils.js 的 tabKey）：P|a/b.md
  ✅ K 段沙箱就绪：viewers.js 真身 + panel.js 七个真身方法（含 menuCloseAll）+ tabs.js 源码可变异
  ✅ edit-md 真身在 K 沙箱里跑了起来（真跑组件代码，不是正则猜想）
  ✅ 组件 mounted() 接上了两条线：md:interrupt（退场）与 v3:editor-host（舱位没了）
  ✅ 打开 md → 进内嵌编辑态（宿主说舱位可用 ⇒ mode=inline，正文已载入）
  ✅ 编辑态已开 → 宿主布局状态变成 editor，内容区类含 v3-content--editor（实测：v3-content v3-content--editor）
  ✅ 输入后进入未保存状态（这一步是后面所有断言的场景前提）
  ✅ 【第一优先·丢内容】发出保存排在编辑态退场【之前】（实测顺序：api:PUT → open=false）
  ✅ 【顺序流水可复核】保存意图 → 数据捕获 → 编辑态退场（实测 kind 顺序：save-intent → save-capture → editor-close）
  ✅ 流水号递增确认「捕获早于卸载」：intent seq=1、capture seq=2、close seq=3
  ✅ viewers 的顺序不变式（先卸后存）零违规
  ✅ 【不变式 2·不误伤】这一发来自裸 emit（source 为空）⇒ 记账但**不判违规**（实测 source=(空)，missing=0）
  ✅ 捕获那一笔按宿主自己的口径记了 key（实测：P|a.md / privhub-files-edit-md）
  ✅ 【捕获的就是要写盘的那份】PUT 的 path 与正文都取自快照（实测 path=a.md）
  ✅ 退场后编辑态关闭（open=false / mode=float）
  ✅ 退场后宿主布局状态归位（不留半截编辑态布局）
  ✅ 整个场景没有异常与 console.error
  ✅ 【静默丢字·修掉的那条路】上一轮的回包不写回新一轮：编辑器仍是 B 的正文（实测 doc="# B 用户刚输入"、baseMtime=555）
  ✅ 新一轮的「未保存」标记不被上一轮的成功回包清掉（实测 dirty=true）
  ✅ a 的正文按 a 的路径写（实测 path=a.md）
  ✅ 宿主收走舱位 → 编辑器当场退场（不然 teleport 会攥着已销毁的目标）
  ✅ 退场之前先把保存发出去了（实测新增 PUT 1 次）
  ✅ 发出去的正是那份未保存的正文
  ✅ 宿主对「能不能内嵌」的否定回答（reason=ask）不会关掉正在编辑的东西
  ✅ 静默保存失败会明确告警（实测 toast：["⚠ 未保存：e.md —— 磁盘满了（请重新打开该文件后再保存）"]）
  ✅ 内容区正承载 a.md → 宿主答 available=true
  ✅ 【要害】舱位在、但内容区显示的是**别的文件** → 宿主答 available=false（旧判据「别人的 DOM 非空」在这里会给 true，编辑器就贴错画面）
  ✅ 内容区被销毁 → 答 available=false
  ✅ 舱位不在（内容区没渲染）→ 答 available=false
  ✅ 舱位消失 → 编辑态布局当场作废（不留半截状态）
  ✅ 只有编辑态时：v3-content v3-content--editor
  ✅ 只有 viewer 时：v3-content v3-content--viewer
  ✅ 两者都不在场时：v3-content
  ✅ 宿主 relay 真身把插件报账写进 viewers 流水（实测：["save-capture@P|a b.md"]）
  ✅ 【兜底】绕开中断入口直接开另一个文件时，上一轮的未保存正文先被捕获并发出保存（实测 path=g.md）
  ✅ 然后才切到新文件（实测 path=h.md）
  ✅ 【清空标签·宿主侧】宿主的 host-gone 通知真的到了（实测流水 kind@reason：["save-capture@host-gone","editor-close@host-gone"]）｜说明：viewer 的 unmount 那一笔不在这条路径上 —— 它由 panel.js 的 `syncViewer` 记，而本段只跑 watcher 的 `syncEditorHost` 那一步（宿主侧三件事里的第三件）。
  ✅ 【清空标签】编辑器随舱位消失当场退场（open=false / mode=float）
  ✅ 【清空标签·顺序】捕获排在编辑态退场**之前**（实测 kind 顺序：save-capture → editor-close）
  ✅ 【清空标签·不丢字】那一发保存真的出去了，且写的是被清空前的那份正文（实测 path=k8.md）
  ✅ 【不误伤】这条路**没有** md:interrupt，但顺序不变式零违规（它查的是倒挂，不是"有没有发过"）
  ❌ 【不变式 2·阳性】真身 tabs.js 的应发路径每走一次都真发了保存意图（实测：["tabs.openTab=发了","tabs.activateTab=发了","tabs.activateTab=发了"]）
  ❌ 四条记录的来源名与 SAVE_INTENT_SOURCES 登记的一致（实测：tabs.openTab → tabs.activateTab → tabs.activateTab）
  ✅ 【不变式 2·零违规】正常时序下 checkSaveIntentCoverage() 无缺项（实测 missing=0，checks 全 seen：tabs.openTab=seen/tabs.activateTab=seen/tabs.closeTab=never）
  ❌ 声明表三行都对上了实际发生的调用（实测：[{"s":"tabs.openTab","seen":true,"emitted":true},{"s":"tabs.activateTab","seen":true,"emitted":true},{"s":"tabs.closeTab","seen":false,"emitted":false}]）
  ❌ 每一发保存意图都记在「当时那个文件」头上（实测 key：P|x.md → P|y.md → P|x.md）
  ❌ 取证点仍认得出「是哪条路发的」（viewers.js 那条 md:interrupt 监听没有失效）—— 实测流水：save-intent → save-intent → save-intent
  ✅ 【阴性 V1·关标签那行被删】那条路这一次**根本没进**（声明表 seen=false），运行期没有"缺项"可报（ok=true）—— 这正是「整段调用被删掉」的运行期盲区，拦它的是段 M3 的源码闸
  ❌ 【阴性 V1b·换成裸 emit】意图照样发出（行为没坏），但没人记账 ⇒ 运行期**查不出来**：如实承认这一条的边界（实测 missing=0、source="(空)"、流水=）
  ❌ 【阴性 V2·把 emit 挪到改 activeKey 之后】意图仍然发出了（这一层没坏：）
  ✅ 【阴性对照 V3·把兜底链断开】宿主不再发 host-gone ⇒ 编辑器**没退场**、也没记流水（实测 open=true、流水=(空)）
  ✅ 【阴性对照 V3】那份未保存正文**一次都没被捕获/发出**——正是 K2-8 守着的那件事（实测新增 PUT 0 次，编辑器里还留着 "# K8 没保存的改动（V3）"）
  ✅ 组件 beforeUnmount 摘掉两条线（插件卸载 → 零残留；viewers.js 自己那条保存意图监听不受影响）
  ✅ 全程零异常、零 console.error
  ✅ panel.js 里找得到铺满判据的纯函数 fillStateFor（判据全取自宿主自己的状态，不查 DOM、不问插件）
  ✅ panel.js 里找得到 contentClass 真身（布局状态类由宿主自己派生）
  ✅ 两个真身都在 vm 里跑了起来（不是正则猜想，是真跑组件代码）
  ✅ 【判据】图片（ready + url）→ 铺满形态：内容区类含 v3-content--fill
  ✅ 【判据】PDF（ready + url）→ 铺满形态：内容区类含 v3-content--fill
  ✅ 【实测形态】是图片却走 iframe 分支的文件（.ico）照样铺满（判据不挂在 kind 上）
  ✅ 【不许误伤】Markdown 正文**不**进铺满形态（用户说的是「除了直接阅读的文件」；.v3-md 的 max-width:900px 是有意留的阅读宽度）
  ✅ 【不许误伤】纯文本正文同样不进铺满形态（只读排版照旧）
  ✅ office 文件照旧走 viewer 契约让位（铺满形态不抢它的活）
  ✅ 还在 loading（没有 url）时不铺满：先按普通布局显示「正在加载…」，拿到内容再铺
  ✅ 内容区承载的**不是**当前标签时（切文件的一瞬）不铺满 —— 判据与模板 v-if 同一口径
  ✅ 真有 viewer 上场时铺满让位（两者互斥，不留两套尺寸规矩同时下给一个容器）
  ✅ 【互斥】viewer 与 fill 同时为真时以 viewer 为准（实测类名：v3-content v3-content--viewer）
  ✅ 三种形态都不在场时仍是裸 v3-content（既有布局一字未变）
  ✅ 【样式】铺满规则写在宿主自己的 styles.js（.v3-content--fill），且去掉了内容区那一圈内边距（实测：.v3-content--fill { display:flex; flex-direction:column; padding:0; overflow:hidden; }）
  ✅ 【样式】迁前那句 height:calc(100vh - 260px) 写死高度已消失（PDF 高度改由容器给）
  ✅ 【样式】PDF 的 1px 相框、8px 圆角与写死高度三件装饰/限制都从基础规则里去掉了（实测：
.v3-pdf { width:100%; height:100%; border:0; display:block; }）
  ✅ 【样式】图片基础规则只留「不超出容器」（宽高都限），装饰性圆角去掉（实测：
.v3-img { max-width:100%; max-height:100%; }）
  ✅ 【不许误伤】文本阅读排版一字未动（.v3-md 的 max-width:900px 仍在）
  ✅ 图片外层改成宿主自己的类（不再靠内联 text-align:center），图片仍可点开放大（lightbox 未被弄坏）
  ✅ 铺满状态由宿主自己的响应式 data（fillLayout）承载，并在 viewer 派生之后同步（顺序有意义：判据之一是「没有 viewer 在场」）

── M 批注锚点 ← 宿主交来的渲染根（第二步 d：跨插件 DOM 认领归零）──
  ✅ 【要害】comments 的代码里不再出现宿主的内容区容器 .v3-content（剥注释后 0 命中）
  ✅ 【要害】comments 的代码里不再按类名去查宿主的渲染根 .v3-md / .v3-text（剥注释后 0 命中）
  ✅ 【要害·加严】comments 整个文件（含注释）grep 不到 querySelector(".v3-content") —— 0 命中
  ✅ comments 不再从整个 document 查任何东西（自己的锚点在自己拿到的根里查，别家的一律不问）
  ✅ 【要害·本批的第二个要害】MutationObserver 已删除：不再"盯着别人的 DOM 变了"重建锚点（剥注释后 0 命中）
  ✅ 锚点根改成**宿主交进来的引用**（data.mdRoot，mdEl 只读它）——不是 computed 里现查 DOM
  ✅ comments 听宿主的新交接事件 v3:md-root（宿主给根/收根都走这一条）
  ✅ 旧的 v3:md-rendered 监听已删除（它发在 Vue 渲染**之前**，那时新节点还不存在 —— 锚点认错根就是这么来的）
  ✅ 根被收回（el 为 null）时不渲染：旧节点连同旧锚点一起作废，锚点不可能残留
  ✅ 组件 beforeUnmount 里放弃根引用（插件被卸载 → 零残留）
  ✅ 宿主内容区里那两个 md 渲染分支（office 提取 / 普通 markdown）都挂了 ref="mdRoot"（宿主自己的节点，不按类名去查、也不新建）｜实测 2 处
  ✅ 宿主有「取当前渲染根」的单一出口（mdRootOf(this.$refs)）
  ✅ 宿主在 $nextTick（= Vue 渲染完成、v-html 已换过内容的那个时点）之后才发根 —— 不是"发完再等 DOM"
  ✅ 宿主发出的 payload 就是渲染根的**节点引用**（el）+ 这一刻的文件 key
  ✅ 宿主两个内容切换入口（换文件 / 切标签）都走到交接：拿不到根就发 el:null（旧锚点随旧节点作废）
  ✅ content.js 里那两发 v3:md-rendered 已删除（它发在 store.content 赋值之后、Vue 渲染之前 —— 监听方只能看到旧节点）
  ✅ 只读引用者 office-ui 核过：干净（既不查宿主内容区、也不往里写）｜扫了 1 个文件
  ✅ 只读引用者 dataview 核过：干净（既不查宿主内容区、也不往里写）｜扫了 2 个文件
  ✅ 只读引用者 mdpage 核过：干净（既不查宿主内容区、也不往里写）｜扫了 1 个文件
  ✅ 只读引用者 versions 核过：干净（既不查宿主内容区、也不往里写）｜扫了 1 个文件
  ✅ 只读引用者 publish 核过：干净（既不查宿主内容区、也不往里写）｜扫了 1 个文件
  ✅ 只读引用者 invite 核过：干净（既不查宿主内容区、也不往里写）｜扫了 1 个文件
  ✅ 图片 / PDF 分支仍只有宿主在管（没有任何插件去查 .v3-img / .v3-pdf）｜实测 0 家
  ✅ comments 不认任何舱位类名（.v3-viewer-host / .v3-editor-host 都不是它的落点 —— 挤进现有舱位算越权）
  ✅ comments 不复用两个状态类（--viewer / --editor 各自会隐藏 .v3-md/.v3-text，那是宿主与 viewer/编辑器的事）
  ✅ 两个舱位各自仍只被它的正主使用（viewer 的地界归 office2、编辑态的地界归 edit-md）
  ✅ comments 真身在沙箱里跑了起来，且锚点根的 watcher 就是 data.mdRoot 上那一个（真跑，不是正则猜想）
  ✅ mounted() 接上了两条线：file:comments（打开面板）与 v3:md-root（渲染根交接）
  ✅ comments 不碰编辑舱位那两条线（舱位归 edit-md，不是本插件的地界）
  ✅ 【行为·渲染 → 通知 → 锚点出现】a.md 渲染完、宿主交出渲染根 → 根里出现 1 个批注锚点（实测 1 个）｜锚点 id=c1
  ✅ 锚点按「相对正文的字符偏移」落在正确位置（start=2/end=5 → "cde"，实测 "cde"）
  ✅ comments 的取数走的是自己的接口（实测调过：1 次）
  ✅ 锚点只渲染在**宿主交来的那个根**里（mdRoot === 根引用）
  ✅ 【行为·换文件】宿主收回渲染根（el:null）→ 插件当场放弃旧引用（不攥着已销毁的目标）
  ✅ 换文件前旧根里确实有锚点（实测 1 个）—— 断言不是空转
  ✅ 【行为·新文件没有锚点】b.md 没有评论 → 新根里 0 个锚点（实测 0 个）
  ✅ 旧根不再被写（换文件后插件一次都没碰旧节点：锚点数仍是 1，没有增/减）
  ✅ 切回 a.md（宿主再交一次根）→ 锚点在**新节点**里重建（实测 1 个）
  ✅ 宿主也不按 .v3-md 类名去查自己的渲染根（它有 ref="mdRoot"；宿主查自己的容器是它的权利，但这里连它也不用）
  ✅ beforeUnmount 摘掉两条线（插件卸载 → 零残留）
  ✅ beforeUnmount 后不再持有渲染根引用

── M 内容区总线事件总表 ↔ 实际代码 ／ 保存意图的源码闸 ──
  ✅ viewers.js 头部有「总线事件总表」（表里的事件名与说明都是**从源码里解析出来的**，不是测试另抄一份）
  ✅ 总表解析出 27 行（覆盖：谁发谁收 / 载荷 / 语义 / 谁的地界）
  ✅ 分槽正确：bus=9 条、out=16 条、info=1 条、retired=1 条（info/retired 不进名单）
  ✅ 总表每一行都写了「方向 / 载荷 / 语义 / 地界」四列（缺一列就算没写清）
  ✅ 任务点名的事件全在表里（含 `v3:viewer-host` 那一行「不存在」的记录）：漏了 无
  ✅ `v3:viewer-host` 如实记为「不存在这个事件」（viewer 的挂载点是选择器 `.v3-viewer-host`，不是事件）—— 不许编一个不存在的总线出来充数
  ✅ 【互相覆盖·漏列】代码里出现的总线事件都在总表里：漏了 无（扫了 15 个文件、共 25 个事件名）
  ✅ 【互相覆盖·多列】总表里 `bus`/`out` 的名字都在代码里真有 emit/on：多了 无
  ✅ 标 `retired` 的事件名确实不在代码里（`v3:md-rendered` ⇒ 0 处 emit / 0 处 on）
  ✅ 【常量发的名字也算数】`v3:editor-host-ask` 在 edit-md 里是 `EVENT_ASK` 常量、在 panel.js 里是字面量 ⇒ 扫描器扫得到，表里也必须有它
  ✅ viewers.js 的 SAVE_INTENT_SOURCES 解析出 3 条声明（只解析那段数组；每条都带 source / file / before）
  ❌ 声明表与实际代码对得上（每条 `before` 都在目标文件里、且排在改 store.activeKey 之前）：tabs.closeTab：`before` 在 tabs.js 里找不到（代码改了没改表？）
  ✅ 声明表点名的是 tabs.js 的三条切标签路：tabs.openTab → tabs.activateTab → tabs.closeTab
  ✅ 【有意例外不入表】`panel.js` 的 `menuCloseAll` 真身里确实**没有**保存意图（也不许被顺手加上）—— 它靠舱位兜底链（段 K2-8 守着）：menuCloseAll() { store.menu = false store.tabs = [] store.activeKey = '' store.content = n…

── L 前端模块语法体检（每个插件前端文件都必须能解析）──
  ✅ 扫到 65 个插件前端文件（覆盖全部 client/*.js）
  ✅ 每个前端模块都能解析（模板串里的反引号这类「整块打不开」的语法错在这里当场拦住）

========================================================
  个人空间界面回归：298 通过 / 10 失败
========================================================
