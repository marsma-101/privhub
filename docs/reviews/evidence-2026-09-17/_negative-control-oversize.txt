阴性对照：把后端改动回退后，新断言必须变红
======================================================

对照做法（只改后端一处，其余不动）：
  临时把 privhub/plugins/privhub-core/src/index.ts 里「超限分支」的返回
  从 { data:'', type:'too-large', size, limit } 改回 { data:'', type:'unknown' }
  （即修复前的原样），然后跑：

    cd privhub; node tests/preview-limits.mjs

命令与输出（原样抄录；EXITCODE=1）：

══ 预览体积上限常驻断言（隔离实例，不碰真实数据）══
  ✅ 隔离实例就绪（端口 3196）
  ✅ 管理员登录成功（取会话令牌）

── ⓪ 探针自证 ──
  ✅ 限内探针确实在 512 KB 上限之下（539 字节）
  ✅ 超限探针确实越过了 512 KB + 20 字节的判定边界（1308467 字节）

── ① 超限文本：给出可区分的超限原因，而不是笼统的 unknown ──
     [实测] PV超大文本.md（1308467 字节）→ {"ok":true,"type":"unknown","data":""}
  ✅ 超限文本的预览接口正常作答（HTTP 200）
  ❌ 超限文本【不再】报成与"类型不支持"同一个值（type=unknown，旧行为是 unknown）
  ❌ 超限文本给出明确的超限类型（type=unknown）
  ❌ 回带实际体积且与磁盘一致（size=undefined，期望 1308467）
  ❌ 回带在线查看上限且确实被超出（limit=undefined 字节）
  ✅ 超限时不把整份文件塞进 JSON（上限本身要保留，不能靠取消上限了事）

── ② 限内小文本：照常可读（修复没有误伤正常文件）──
  ✅ 限内小文本预览仍是 ok=true
  ✅ 限内小文本仍是可渲染的文本类型（type=text）
  ✅ 限内小文本的内容照常返回（不是空壳）
  ✅ 限内小文本不追加超限字段（响应形状对正常路径无新增噪音）

── ③ 真正不支持的扩展名：仍是明确的"不支持" ──
  ✅ 不支持类型仍以 200 + ok 作答（不改成报错，兼容既有前端分支）
  ✅ 不支持的扩展名仍是 unknown（type=unknown）
  ✅ 不支持类型不带 size/limit（两种情形在响应上也是分开的）
  ❌ 断言核心：超限与不支持给出【不同】的 type（unknown ≠ unknown）

── ④ 出路：下载通道对超限文件照常可用（界面指向的那条路真的通）──
  ✅ 超限文件仍可下载（HTTP 200）
  ✅ 下载内容长度与磁盘一致（1308467，期望 1308467）

========================================================
  预览体积上限断言：15 通过 / 5 失败
========================================================

EXITCODE=1

结论：断言确实钉在【行为】上 ——
  · 回退后接口又变回 {"ok":true,"type":"unknown","data":""}（与父级实测的 1.6 MB md 同形）；
  · 5 条转红，其中「超限与不支持给出不同 type」正是本缺陷的要害断言；
  · 而「限内小文本照常」「不支持仍是不支持」「下载通道可用」这 15 条保持绿 ——
    说明变红的不是环境、不是探针，正是被修的那一处行为。

对照做完已整份还原：privhub/plugins/privhub-core/src/index.ts 的超限分支
恢复为 { data:'', type:'too-large', size: s.size, limit: maxTextBytes }，
还原后 `node tests/preview-limits.mjs` 为 20 通过 / 0 失败（见 CHANGELOG 同段「证据」）。
